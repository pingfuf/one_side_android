<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval" >

    <!-- color in widget -->
    <solid android:color="@color/white" />
    <!-- set color and sieze of boundary -->
    <!--
             android:dashGap="0.3dp"
        android:dashWidth="0.5dp"
    -->
    <stroke
        android:width="1dp"
        android:color="@color/layout_background" />

</shape>