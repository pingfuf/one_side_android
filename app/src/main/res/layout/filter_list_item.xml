<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_content"
    android:layout_width="match_parent"
    android:layout_height="@dimen/expand_view_title_height"
    android:background="@drawable/filter_item_background"
    android:minHeight="@dimen/expand_view_title_height" >

    <TextView
        android:id="@+id/tv_expand_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/expand_view_title_height"
        android:gravity="center"
        android:text="test"
        android:textColor="@color/text_color_gray_black"
        android:textSize="@dimen/text_size_sub_title" >
    </TextView>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/line_height"
        android:layout_alignParentBottom="true"
        android:background="@color/line_gray" />

</RelativeLayout>